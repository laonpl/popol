# FitPoly 사이트 성능·접근성 개선 (Lighthouse 기반)

> 출처: `design.md`(라이트하우스 13.3.0 원시 리포트, 2026-06-02, www.fitpoly.kr 4개 페이지). 아래 수치는 전부 그 리포트 실측치이며, 지어낸 값 없음.

## 0. 페이지별 점수 (실측)

| 페이지 | 성능 | 접근성 | 베스트 | SEO | LCP | FCP | Speed Index |
|---|---|---|---|---|---|---|---|
| `/` (홈) | **65** | 83 | 100 | 100 | **22.1s** | 3.0s | 5.6s |
| `/login` | 71 | **72** | 100 | 100 | 22.0s | 2.6s | 2.6s |
| `/privacy` | 74 | 83 | 100 | 100 | 22.0s | 1.7s | 3.0s |
| `/terms` | 69 | 83 | 100 | 100 | 21.4s | 2.4s | 4.8s |

- 잘하는 것: **베스트 프랙티스·SEO 100점**, HTTPS, CLS 0, TBT 0.
- 못하는 것: **LCP가 모든 페이지에서 ~22초**(치명), 접근성 70~83, 성능 65~74.

---

## 1. ★ 최우선 — LCP 22초의 주범 = 거대한 로고 이미지 〔수정 완료〕

- **문제**: `/logo.png`가 **2048×2048, 3.47MB(3,634,398 bytes)** 인데 실제 화면에선 **최대 52px**(헤더 32px, 랜딩 20px, 로그인 40px, 부팅 52px)로만 표시됨.
  - 이 한 장이 **페이지 전체 용량의 87%**(4,071KiB 중 3,551KiB).
  - LCP 요소이자 22초 LCP의 직접 원인. OG 공유 이미지로도 3.5MB라 비효율.
- **수정(완료)**: `frontend/public/logo.png`를 **512×512 / 165KB**로 재인코딩(**95%↓**). 원본은 git 히스토리에 보존(`git show HEAD:frontend/public/logo.png`로 복구 가능).
- **기대 효과**: LCP 22s → 수초 내로 급감, 총 페이지 용량 ~4MB → ~0.7MB. **성능 점수 대폭 상승 예상**.
- **권장(후속)**: 더 줄이려면 WebP(투명 지원)로 256~384px 재서빙 시 ~30KB까지 가능. 빌드에 이미지 최적화(예: `vite-plugin-image-optimizer`) 도입 검토.

---

## 2. P1 — 빠르게 잡을 큰 항목

### 2-1. 다중 리다이렉트 (~760ms, 전 페이지)
- `https://fitpoly.kr/` → `https://www.fitpoly.kr/` 2단계 리다이렉트로 ~760ms 손해.
- **조치**: 호스팅/DNS에서 apex(fitpoly.kr)를 www로 **단일 301**로 정리하거나, 반대로 www→apex 한 방향만. Firebase Hosting이면 `firebase.json` redirects 또는 도메인 설정에서 1-hop으로.

### 2-2. 색 대비 부족 (접근성) — 홈 **63건**, 로그인 3건 등
- 본문 회색 텍스트가 배경 대비 **2.53:1**(기준 4.5:1 미달)인 곳 다수. 예: `bg-white` 위 `text-[13px]` 회색, `bg-[#aec5d8]` 위 흐린 글자.
- **조치**: 연한 `bluewood-300/400`·`text-gray-400`을 본문에 쓰는 곳을 **최소 `bluewood-500` 이상**으로 상향. 특히 랜딩의 카드 설명문/캡션. (디자인 결정 필요 → 토큰 단위로 일괄 조정 권장)

### 2-3. 접근성 구조
- **`<main>` 랜드마크 없음**(전 페이지): 최상위 콘텐츠를 `<main>`으로 감싸기(Layout/페이지 래퍼).
- **버튼 접근명 없음**(로그인 1건): 아이콘/빈 버튼에 `aria-label` 추가.
- **heading-order**(홈): h1→h3 건너뜀 등 순서 정리.
- **링크가 색으로만 구분**(약관/개인정보): 본문 링크에 밑줄 등 비색상 단서.
- **target-size**: 터치 타깃 44px 미만 4건 → 작은 버튼 패딩/영역 확대.

---

## 3. P2 — 번들/캐시 최적화 (각 100~250KB·수백 ms)

| 항목 | 규모 | 조치 |
|---|---|---|
| 미사용 JS | ~136~230KiB | 라우트 코드스플리팅 강화, 미사용 vendor 정리(yoopta/slate 등 지연 로드) |
| 미압축 JS | ~27~36KiB | 빌드 minify 확인(서드파티/iframe 스크립트 점검) |
| 미사용 CSS | ~16~19KiB | Tailwind purge 범위 확인 |
| 렌더 블로킹 | ~150ms | CSS/스크립트 defer·preload 조정 |
| 캐시 수명 | ~78KiB | 정적 자산 `Cache-Control` 장기 캐시 헤더 |
| unsized-images | 홈 2건 | `<img>`에 `width`/`height` 명시(레이아웃 안정) |

- 참고: 큰 vendor 청크(firebase 109KB, yoopta 85KB, slate 55KB)는 **로그인/에디터에서만** 필요하므로, 랜딩·약관 등에는 로드되지 않도록 라우트 분리 점검.

---

## 4. 권장 실행 순서
1. **〔완료〕 로고 경량화** — 단일 변경으로 LCP·용량 최대 효과.
2. 리다이렉트 1-hop 정리(호스팅 설정).
3. 색 대비 토큰 일괄 상향 + `<main>`·`aria-label`·heading 순서(접근성 70→90+ 목표).
4. 번들 코드스플리팅/캐시 헤더로 성능 추가 상승.
5. 재측정(Lighthouse) → 개선폭 확인.

> 다음 단계로 원하시면 **2-2 색 대비**(프론트 토큰 일괄 조정)와 **2-3 `<main>`/`aria-label`**을 코드로 바로 반영하겠습니다. 리다이렉트·캐시는 호스팅 설정 영역이라 `firebase.json`/도메인 설정을 함께 보면 정리해 드릴 수 있어요.
