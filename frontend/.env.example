# ─────────────────────────────────────────────
# Frontend 환경변수 (Vercel 대시보드 → Settings → Environment Variables에 입력)
# ─────────────────────────────────────────────

# 백엔드 API URL (Render 배포 후 설정) ★ 필수
VITE_API_URL=https://your-backend.onrender.com/api

# Firebase 설정은 src/config/firebase.js에 하드코딩되어 있어 별도 설정 불필요
