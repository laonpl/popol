# ─────────────────────────────────────────────
# Backend 환경변수 (Render 대시보드 → Environment에 입력)
# ─────────────────────────────────────────────

# 서버 포트 (Render는 자동 주입, 기본 10000)
PORT=5000

# 프론트엔드 URL (Vercel 배포 후 설정) - CORS 허용 도메인
FRONTEND_URL=https://your-app.vercel.app

# Vertex AI / Gemini 설정
# API 키만 쓰면 Vertex AI Express 모드로 호출됩니다.
# 서비스계정/ADC로 표준 Vertex AI를 쓰려면 VERTEX_AI_AUTH_MODE=adc 를 추가하세요.
VERTEX_AI_PROJECT_ID=popol-495305
VERTEX_AI_LOCATION=asia-northeast3
GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Admin SDK - serviceAccountKey.json 파일 내용을 한 줄 JSON으로 붙여넣기
# 예: {"type":"service_account","project_id":"...","private_key":"-----BEGIN RSA PRIVATE KEY-----\n..."}
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}

# Notion 통합 토큰 (내보내기 기능 사용 시)
NOTION_INTEGRATION_TOKEN=secret_xxx

# ─────────────────────────────────────────────
# 무료 티어(예: Render 512MB) 보호 스위치 — 권장 ON
# ─────────────────────────────────────────────
# 채용공고 URL 스크래핑 시 Chromium(Puppeteer) 실행을 끔 → OOM 방지.
# 끄면 HTTP 스크래핑/Gemini 검색 기반 분석으로 자동 전환되어 기능은 계속 동작.
PUPPETEER_DISABLED=true
# 이미지 OCR을 로컬 Tesseract 대신 Gemini Vision API로 처리 → OOM 방지.
OCR_PREFER_VISION=true
# (빌드 환경) Puppeteer 설치 시 Chromium 다운로드 생략 → 배포 용량/시간 절감.
PUPPETEER_SKIP_DOWNLOAD=true
