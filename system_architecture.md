# 🏢 POPOL - 시스템 아키텍처 및 모듈 명세서 (Head-Track 구조)

이 문서는 POPOL 프로젝트의 전체 시스템 블록 구성, 핵심 로직에서 사용되는 기술 스택(언어 및 프레임워크), 그리고 각 모듈별 주요 기능을 `헤드-트랙(Head-Track)` 형식으로 정리한 가이드입니다.

---

## 🎯 1. 전체 시스템 블록 구성도 (System Block Diagram)

본 구성도는 Frontend(React)와 Backend(Node.js/Express) 간의 상호작용 및 Firebase/Gemini AI 서버와의 연결 구조를 나타냅니다. 클라이언트와 서버가 역할을 분담하며, 무거운 연산(AI 처리, 파일 검증 등)은 백엔드에서 전담하는 구조를 가집니다.

```text
┌──────────────────────────────────────────────────────────┐
│                    Frontend (React)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │ 경험정리  │  │포트폴리오│  │ 자소서   │              │
│  │ (STAR,5F │  │ (에디터  │  │ (AI초안  │              │
│  │  PMI,KPT │  │  검증/Export│ │  글자수) │              │
│  │  4L)     │  │          │  │          │              │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘              │
│       │              │              │                    │
│  ┌────┴──────────────┴──────────────┴────┐              │
│  │         Zustand Store (상태관리)       │              │
│  │  authStore | experienceStore |        │              │
│  │  portfolioStore | coverLetterStore    │              │
│  └────────────────┬──────────────────────┘              │
│                   │                                      │
│  ┌────────────────┴───────────────────────┐             │
│  │     Firebase Client SDK (직접 접근)    │             │
│  │  Firestore CRUD | Auth | Storage       │             │
│  └────────────────┬───────────────────────┘             │
└───────────────────┼──────────────────────────────────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
   ┌────┴────┐ ┌───┴────┐ ┌───┴──────┐
   │Firebase │ │Firebase│ │Firebase  │
   │  Auth   │ │Firestore│ │Storage  │
   └─────────┘ └────────┘ └──────────┘
                    │
        ┌───────────┘ (AI 분석 및 파일 검증 로직 처리)
        │ 
┌───────┴──────────────────────────────────────────────────┐
│                   Backend (Express)                      │
│  ┌──────────────────────────────────────────┐            │
│  │  Auth Middleware (Firebase ID Token 검증) │            │
│  └──────────────┬───────────────────────────┘            │
│                 │                                        │
│  ┌──────────────┴───────────────────────────┐            │
│  │              Routes                      │            │
│  │  /experience/analyze   → Gemini AI 분석  │            │
│  │  /portfolio/validate   → 체크리스트 검증  │            │
│  │  /portfolio/export     → 멀티포맷 변환    │            │
│  │  /coverletter/generate → AI 자소서 초안   │            │
│  └──────────────┬───────────────────────────┘            │
│                 │                                        │
│  ┌──────────────┴───────────────────────────┐            │
│  │        AI Service (Gemini API)           │            │
│  │  역량/키워드 추출 | 포트폴리오 검수 | 자소서 생성│            │
│  └──────────────────────────────────────────┘            │
└──────────────────────────────────────────────────────────┘
```

---

## 🛠 2. 핵심 로직에 사용되는 언어 및 프레임워크

### 🎨 Frontend
*   **언어**: JavaScript (ES6+), JSX
*   **프레임워크 및 라이브러리**:
    *   **React (v18.2)**: UI 컴포넌트 렌더링 및 SPA(Single Page Application) 페이지 구성
    *   **Vite**: 빠른 개발 서버 구동 및 실전용 번들링
    *   **TailwindCSS**: 유틸리티 클래스 기반 디자인 시스템 반영 및 반응형 스타일링 적용
    *   **Zustand**: 전역 상태 관리 (인증, 경험 기록, 포트폴리오, 자소서 등의 복잡한 상태 분리 관리)
    *   **React Router DOM**: 페이지별 라우팅 처리
    *   **@yoopta/editor & Slate**: 블록 기반 포트폴리오 에디터 (Rich Text Editor) 구축
    *   **@hello-pangea/dnd**: 드래그 앤 드롭 기능을 통한 직관적인 UI 요소 조작
    *   **pdfjs-dist / pptxgenjs / jspdf / html2canvas**: 클라이언트 사이드에서의 문서 렌더링 및 내보내기 지원

### ⚙️ Backend
*   **언어**: JavaScript (Node.js 20+)
*   **프레임워크 및 런타임**:
    *   **Node.js & Express**: RESTful API 서버 구축 및 클라이언트 요청 라우팅
    *   **Firebase Admin SDK**: 서버 환경에서 클라이언트가 전달한 Firebase ID Token의 유효성 검증
    *   **@google/genai & openai**: AI 모델 연동을 통한 사용자 경험 키워드 추출, 자소서 초안 생성, 포트폴리오 검수
    *   **@notionhq/client**: Notion API 연동을 통한 문서 멀티포맷 Export 기능(노션으로 내보내기) 제공
    *   **Puppeteer / Mammoth / Tesseract.js**: 업로드된 문서 형식 변환, 웹 스크래핑 및 이미지 내 텍스트 인식(OCR) 기능
    *   **Multer**: 대용량 파일 및 문서 업로드 처리

---

## 📦 3. 모듈별 주요 기능 (Head-Track 명세)

### 3.1 👨‍💻 사용자 및 인증 모듈 (Auth Module)
*   **Head (목표/역할)**: 사용자 접근 제어 및 소셜 로그인 세션 관리
*   **Track (핵심 동작/흐름)**:
    *   `Firebase Auth`를 이용하여 Google OAuth 기반 로그인을 수행합니다.
    *   로그인 완료 후 `Firebase ID Token`을 클라이언트에 캐싱하고, 백엔드 API 요청 시 Authorization 헤더(Bearer)로 전달합니다.
    *   백엔드의 `auth.js` 미들웨어에서 ID 토큰을 파싱 및 검증하여 인가된 사용자만 민감한 API를 호출할 수 있도록 통제합니다.

### 3.2 📝 경험 정리 모듈 (Experience Module)
*   **Head (목표/역할)**: 사용자 경험의 체계적 기록 유도 및 AI 기반 역량 분석
*   **Track (핵심 동작/흐름)**:
    *   사용자는 제공되는 프레임워크 템플릿(STAR, 5F, PMI, KPT, 4L)을 통해 경험을 기록합니다.
    *   입력된 데이터는 Backend의 `/experience/analyze` 엔드포인트로 전송됩니다.
    *   **Gemini AI** 프롬프팅을 거쳐 경험 내용 중 핵심 문장을 하이라이팅하고 직무 역량 키워드를 자동으로 추출합니다.
    *   AI가 분석한 최종 결과물은 Firestore Database에 저장되며, Frontend의 `experienceStore`를 통해 UI와 동기화됩니다.

### 3.3 📁 포트폴리오 모듈 (Portfolio Module)
*   **Head (목표/역할)**: 맞춤형 포트폴리오 리치 에디팅 및 멀티포맷(PDF/Notion/Github) Export
*   **Track (핵심 동작/흐름)**:
    *   `@yoopta/editor`를 통해 이력서 섹션(소개, 경험, 기술, 학력 등)을 블록 형태로 자유롭게 구성합니다.
    *   Firestore의 Experience DB에 저장된 내용을 조회하여 에디터 내 특정 섹션과 **직접 연동(Mapping)** 할 수 있습니다.
    *   **6단계 체크리스트 판별 시스템**:
        1. 프론트엔드 레벨 검증: 네이밍 룰 체크 및 포맷 확장자 확인
        2. 백엔드 레벨 검증: 파일 용량 체크
        3. AI 검증: 지원 기업/직무 매칭도 분석, 기여도 명시 확인, 오타 및 비문 검수
    *   모든 체크리스트 항목(6개)을 통과하면 최종 Export 버튼이 활성화되어 타겟 포맷(PDF, 노션 등)으로 변환(`/portfolio/export`)됩니다.

### 3.4 ✍️ 자기소개서 모듈 (CoverLetter Module)
*   **Head (목표/역할)**: AI 기반 맞춤형 자소서 초안 작성 및 글자 수 제한 관리
*   **Track (핵심 동작/흐름)**:
    *   사용자가 목표 기업, 지원 직무, 그리고 자소서 문항을 입력합니다.
    *   각 문항과 관련된 기존의 '경험 내역'을 드래그 앤 드롭으로 연결(linkedExperienceIds)합니다.
    *   선택된 경험 데이터셋과 문항 의도를 함께 Backend의 `/coverletter/generate` 엔드포인트로 전송합니다.
    *   **Gemini AI**가 문항의 의도를 파악하고, 연결된 경험을 자연스럽게 녹여낸 자소서 초안을 생성해 반환합니다.
    *   에디터 상단에 실시간 글자 수 카운팅 정보를 제공하여 제출 분량 규격을 맞출 수 있도록 지원합니다.
